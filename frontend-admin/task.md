# Tailwind Port Implementation Tasks

- [x] Install Tailwind dependencies (`tailwindcss`, `postcss`, `autoprefixer`).
- [x] Initialize `tailwind.config.js` and `postcss.config.js` with correct custom config.
- [x] Update `src/index.css` to use Tailwind directives.
- [x] Update `index.html` to include custom fonts and icon imports.
- [x] Port `main_admin_dashboard` HTML to `AdminLayout.tsx` and `Dashboard.tsx`.
- [x] Delete `AdminLayout.css` and migrate any other custom styling.
- [x] Port `inventory_dashboard` HTML to `Inventory.tsx`.
- [x] Port `order_management_approval` HTML to `Orders.tsx`.
- [x] Port `credit_review_queue` HTML to `CreditApps.tsx`.
- [x] Port `system_settings_audit` HTML to `Settings.tsx`.
- [x] Port `admin_login_otp` HTML to `Login.tsx`.
